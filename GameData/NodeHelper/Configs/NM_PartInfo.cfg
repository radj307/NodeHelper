// Same as the PartInfo mod, so if it is here, no need to define it
//
@PART[*]:HAS[~name[TCAModule*],~name[Deployed*]]:NEEDS[!PartInfo]:FINAL
{
	MODULE
	{
		name = ModuleNHPartInfo

		originalPartName = #$../name$
	}
}
